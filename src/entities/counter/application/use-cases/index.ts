import { decrementValue } from './decrement-value';
import { getInitialValue } from './get-initial-value';
import { incrementValue } from './increment-value';

export const UseCases = {
  getInitialValue,
  incrementValue,
  decrementValue,
};
